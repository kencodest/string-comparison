{
  "name": "string-comparison",
  "version": "1.0.0",
  "description": "This is an implementation of string comparison using the string-similarity node package.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kennedy Muia",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
